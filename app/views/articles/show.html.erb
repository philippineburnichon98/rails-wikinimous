<h2><%= @article.title %></h2>
<p><%= @article.content %></p>
<%= link_to "Delete the Article",
            article_path(@article),
            method: :delete,
            data: { confirm: "Are you sure?" } %>
<%= link_to "Back to list",
            "/articles" %>,

<%= link_to "Update the Article",
            edit_article_path %>
